version: '3.8'

services:
  evolution-api:
    image: atendai/evolution-api
    container_name: evolution-api

    ports:
      - "8080:8080"

    environment:
      - SERVER_TYPE=http
      - SERVER_PORT=8080
      - AUTHENTICATION_API_KEY=123456

    restart: always